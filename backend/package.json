{
  "name": "restoration-roundrobin-backend",
  "version": "1.0.0",
  "description": "Restoration lead-gen backend with vendor round-robin call forwarding",
  "main": "server.js",
"scripts": {
  "start": "node server.js",
  "dev": "nodemon server.js",
  "init-db": "node init-db.js",
  "postinstall": "cd ../frontend && npm install && npm run build",
  "railway-start": "node init-db.js && node server.js"
},
  "dependencies": {
    "express": "^4.19.2",
    "better-sqlite3": "^11.3.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "twilio": "^5.3.0",
    "express-rate-limit": "^7.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
