{
  "name": "restoroute",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && node init-db.js && node server.js"
  }
}